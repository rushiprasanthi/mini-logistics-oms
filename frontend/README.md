# Frontend - Mini Logistics OMS

This is a lightweight React + Vite frontend for the Mini Logistics OMS backend.

Environment:
- Vite
- React
- TailwindCSS

To run locally:
1. npm install
2. Copy `.env.example` to `.env` and set `VITE_API_BASE` if needed
3. npm run dev
